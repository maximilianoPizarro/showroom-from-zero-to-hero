<link rel="stylesheet" href="{{uiRootPath}}/css/site-extra.css">
<script src="https://cdn.jsdelivr.net/npm/mermaid@10.9.0/dist/mermaid.min.js"></script>
<script>
document.addEventListener('DOMContentLoaded', function() {
  mermaid.initialize({ startOnLoad: false, theme: 'default', securityLevel: 'loose' });
  var keywords = /^\s*(graph\s|flowchart\s|sequenceDiagram|classDiagram|stateDiagram|erDiagram|gantt|pie|gitGraph)/;
  var idx = 0;
  document.querySelectorAll('.literalblock pre').forEach(function(pre) {
    var code = pre.textContent.trim();
    if (!keywords.test(code)) return;
    var div = document.createElement('div');
    div.className = 'mermaid-rendered';
    div.style.cssText = 'background:#fff;padding:16px;border-radius:8px;text-align:center;margin:16px 0;';
    var id = 'mermaid-' + (idx++);
    mermaid.render(id, code).then(function(result) {
      div.innerHTML = result.svg;
      pre.parentElement.parentElement.replaceWith(div);
    }).catch(function(err) { console.warn('Mermaid render error for ' + id + ':', err); });
  });
});
</script>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">

<link rel="icon" href="{{uiRootPath}}/img/favicon.ico" type="image/x-icon">

<script>
(function() {
  var VARS = [
    { key: 'CLUSTER_DOMAIN', placeholder: '__CLUSTER_DOMAIN__', label: 'Cluster domain', hint: 'apps.cluster-xxx.dynamic.redhatworkshops.io' },
    { key: 'USER_NAME',      placeholder: '%USER_NAME%',        label: 'Username',       hint: 'user1' }
  ];

  function esc(s) { return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&'); }
  function get(k) { return localStorage.getItem(k) || ''; }
  function set(k,v) { localStorage.setItem(k, v); }

  function getSearchParams() {
    var params = new URLSearchParams(window.location.search);
    try {
      if (window.parent && window.parent !== window) {
        var pp = new URLSearchParams(window.parent.location.search);
        pp.forEach(function(val, key) { if (!params.has(key)) params.set(key, val); });
      }
    } catch(e) {}
    return params;
  }

  function replacePlaceholders() {
    VARS.forEach(function(v) {
      var val = get(v.key);
      if (!val) return;
      var re = new RegExp(esc(v.placeholder), 'g');
      document.querySelectorAll('a[href*="' + v.placeholder + '"]').forEach(function(a) {
        var h = a.getAttribute('href');
        if (h) a.setAttribute('href', h.replace(re, val));
      });
      var walker = document.createTreeWalker(document.body, NodeFilter.SHOW_TEXT);
      var nodes = [];
      while (walker.nextNode()) {
        if (walker.currentNode.textContent.indexOf(v.placeholder) !== -1) nodes.push(walker.currentNode);
      }
      nodes.forEach(function(n) { n.textContent = n.textContent.replace(re, val); });
      document.querySelectorAll('code, pre, strong, em, span, p, li, td').forEach(function(el) {
        if (el.innerHTML.indexOf(v.placeholder) !== -1) el.innerHTML = el.innerHTML.replace(re, val);
      });
    });
  }

  function init() {
    var params = getSearchParams();
    var changed = false;
    VARS.forEach(function(v) {
      var qp = params.get(v.key);
      if (qp) { set(v.key, qp); changed = true; }
    });
    replacePlaceholders();
    [100, 500, 1000, 2000, 4000].forEach(function(ms) { setTimeout(replacePlaceholders, ms); });
  }

  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', init);
  else init();
})();
</script>
<style>
.author-footer{text-align:center;padding:16px 0 8px;margin-top:32px;border-top:1px solid #e0e0e0;font-size:0.75rem;color:#999;}
.author-footer a{color:#999;text-decoration:none;}
.author-footer a:hover{color:#0066cc;text-decoration:underline;}
.author-footer img{height:28px;vertical-align:middle;margin-right:6px;}
.showcase-btn{position:fixed;bottom:16px;right:16px;z-index:9999;background:#fff;border:1px solid #ddd;border-radius:12px;padding:8px 14px;box-shadow:0 2px 8px rgba(0,0,0,0.12);cursor:pointer;display:flex;align-items:center;gap:6px;font-size:0.8rem;color:#333;text-decoration:none;transition:box-shadow 0.2s,transform 0.2s;}
.showcase-btn:hover{box-shadow:0 4px 16px rgba(0,0,0,0.2);transform:translateY(-2px);color:#0066cc;}
.showcase-btn svg{width:20px;height:20px;}
</style>
<script>
document.addEventListener('DOMContentLoaded',function(){
  var doc=document.querySelector('.doc');
  if(doc){
    var f=document.createElement('div');
    f.className='author-footer';
    var logoSrc=document.querySelector('link[rel=icon]')?.href?.replace('/img/favicon.ico','/img/Logo-OSC_with_padding.png')||'';
    f.innerHTML='<a href="https://commons.openshift.org/" target="_blank" rel="noopener"><img src="'+logoSrc+'" alt="OpenShift Commons"></a> '+
      '<a href="https://commons.openshift.org/" target="_blank" rel="noopener">OpenShift Commons Gathering — Buenos Aires 2026</a>';
    doc.appendChild(f);
  }
  var btn=document.createElement('a');
  btn.className='showcase-btn';
  btn.href='https://maximilianopizarro.github.io/test-drive-pe-oscg/';
  btn.target='_blank';
  btn.rel='noopener';
  btn.title='Workshop Showcase — 3D Experience';
  btn.innerHTML='<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2L2 7l10 5 10-5-10-5z"/><path d="M2 17l10 5 10-5"/><path d="M2 12l10 5 10-5"/></svg>3D';
  document.body.appendChild(btn);
});
</script>
